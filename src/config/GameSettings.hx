package config;

class GameSettings {
    public static var SCENE_WIDTH = 800;
    public static var SCENE_HEIGHT = 800;
    public static var SHAPE_MAX_WIDTH = 300;
    public static var SHAPE_STEP_Y = 100;

    public static var GRAVITY_INTERVAL = [1, 10];
    public static var GRAVITY_DEFAULT = 5;
    public static var SHAPES_PER_SEC_INTERVAL = [1, 10];
    public static var SHAPES_PER_SEC_DEFAULT = 1;
}